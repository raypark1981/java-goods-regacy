자동 충전 결제
* 내 역할 Java API 서버 개발
* 작업 내용 정리
    1. OTC 승인 서버가 포인트조회, 충전금액 판단 결제 요청 할 Java 쪽 API(내 작업)
        - 내용1: 토스 PG호출(토스 빌링 결제API호출) 해서 부족한 금액 결제 (내 작업)
          > 내용2 실행
        - 내용2: 결제 성공 여부에 따른 분기 로직(내 작업)
          > 성공시: 포인트 충전 - NICE 호출(포인트 충전 호출) (내 작업)
          > 실패시: OTC 승인 서버 실패 처리(승인서버 api 유무 확인 후 처리 로직 작성)
            - 종료 처리
        - 내용3: 내용2의 포인트 충전 성공 여부에 따른 분기 로직 (내 작업)
          > 성공시: OTC 서버 포인트 사용 요청(승인서버 api 유무 확인)
            - 이후 로직은 OTC 서버로 위임
          > 실패시: 토스 결제 취소 요청
            - 처리 후: 내용2의 실패시 로직 처리
            - 종료 처리

        - OTC 서버에서 NICE 포인트 사용 호출(API쪽 작업 없음)