자동 충전 결제
목표는 토스 빌링 결제 (신규)
1. 계좌로만 결제 진행
2. 계좌만 등록 (카드 제외)
3. 빌링키 등록, 빌링키로 결제

* 내 역할 Java API 서버 개발
* 작업 내용 정리
    1. OTC 승인 서버가 포인트조회, 충전금액 판단 결제 요청 할 Java 쪽 API(내 작업)
       > PEAAutoChargeController생성, 
       - 등록 계좌 조회 및 확인  없을시 계좌 없음 코드 던짐
       - 토스 호출하기전, 충전 예상금액(자동천전-금액-노출기준.md 참조)
       - 토스 PG호출(토스 빌링 결제API호출) 해서 부족한 금액 결제 (등록계좌,빌링키,충전예상 금액)
       - 토스 결제 성공 여부에 따른 분기 로직(내 작업)
        -  성공시: 포인트 충전 - NICE 호출(포인트 충전 호출) 
        -  실패시: OTC 승인 서버 실패 처리(승인서버 api 유무 확인)
            - 종료 처리
        -   포인트 충전 성공 여부에 따른 분기 로직 (내 작업)
            - 성공시: OTC 서버 포인트 사용 요청(승인서버 api 유무 확인)
              - 이후 로직은 OTC 서버로 위임
            - 실패시: 토스 결제 취소 요청
              - 처리 후: 내용2의 실패시 로직 처리
          - 종료 처리

       - OTC 서버에서 NICE 포인트 사용 호출(API쪽 작업 없음)

## OTC 서버 관리자 확인 필요
